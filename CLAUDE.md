# CLAUDE.md

このファイルはClaude Codeが本プロジェクトのセッション開始時に自動で読み込む、作業用コンテキストです。詳細な背景・決定事項は README.md を参照してください。

## プロジェクト概要

Claude Code CLI用の常駐デスクトップペットウィジェット。Claude Codeが応答生成中は「考え中」、応答完了で「笑顔」になるキャラクターを画面右下に表示する。OpenAI Codex CLIの公式ペット機能（`/pet`）に相当する体験を自作するのが目的。

## 現在のステータス

設計フェーズ。コードは未着手（README.mdのみ存在）。

## 確定している技術方針

- **Electron** を採用（Mac以外への展開余地を残すため、Swift/AppKitではなくクロスプラットフォームを優先）
- 画面右下に常時最前面・枠なし・背景透過ウィンドウで表示
- Claude Code CLIが起動しているときだけ表示
- 状態検知はClaude Codeの **hooks機能** を使う想定
  - `UserPromptSubmit` フック → 状態ファイルに `thinking` を書き込む
  - `Stop` フック → 状態ファイルに `smile`（完了）を書き込む
  - Electron側は状態ファイルをポーリング/監視してアニメーションを切り替える

## 未確定・要調査（次にやること)

- Codex CLIのペットが表示されている場合、その隣に配置する機能
  - Codexのペットはターミナル内描画の可能性が高く、OSウィンドウとして位置取得できるか未検証。できない場合は右下固定+近似オフセットで妥協する
- 状態ファイルの具体的なパス・フォーマット設計
- Windows/Linuxでの常時最前面・透過ウィンドウの挙動差異（Macで動作確認後に検証）

## 作業時の留意事項

- ユーザーとのやり取りは日本語で行う
- 大きな設計判断（フレームワーク選定など）は実装前にユーザーに確認する
